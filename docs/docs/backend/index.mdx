---
sidebar_position: 1
---

# Backend: index.js

The backend runs in the Bare runtime (a Node-like environment) and handles all the peer-to-peer communication!

## Key Imports

```javascript
import Runtime from 'pear-electron'
import Bridge  from 'pear-bridge'
import Hyperswarm from 'hyperswarm'
import Hypercore  from 'hypercore'
```

## Booting the App

First, we set up pear-electron and pear-bridge to connect our backend and frontend:

```javascript
const runtime = new Runtime()

const bridge = new Bridge({ mount: '/ui', waypoint: 'index.html' })
await bridge.ready()

const pipe = await runtime.start({ bridge })
Pear.teardown(() => pipe.end())
```

This creates a bridge that serves our `ui/` folder and connects it to our backend via a pipe!

## Message Framing

We send messages between the backend and frontend using newline-delimited JSON:

```javascript
let recvBuf = ''

pipe.on('data', (data) => {
  recvBuf += Buffer.from(data).toString('utf8')
  const lines = recvBuf.split('\n')
  recvBuf = lines.pop()
  for (const line of lines) {
    if (!line.trim()) continue
    try { handle(JSON.parse(line)) } catch (e) { console.error('[BE] bad msg:', e) }
  }
})

function send (msg) {
  pipe.write(JSON.stringify(msg) + '\n')
}
```

This ensures we can send complete messages even if they're split into chunks!

## Peer-to-Peer with Hyperswarm

To connect two players, we use Hyperswarm! Let's look at how hosting works:

```javascript
async function onHost () {
  swarm = new Hyperswarm()

  const topic = Buffer.alloc(32)
  for (let i = 0; i < 32; i++) {
    topic[i] = Math.floor(Math.random() * 256)
  }

  attachSwarmListeners()

  const discovery = swarm.join(topic, { server: true, client: false })
  await discovery.flushed()

  send({ type: 'hosted', code: topic.toString('hex') })
}
```

Joining a room is similar, but as a client:

```javascript
async function onJoin (code) {
  swarm = new Hyperswarm()
  const topic = Buffer.from(code, 'hex')

  attachSwarmListeners()

  swarm.join(topic, { server: false, client: true })
}
```

## Leaderboard with Hypercore

For a persistent, shared leaderboard, we use Hypercore!

```javascript
async function onLbInit () {
  try {
    const storage = Pear.config.storage.replace(/[/\\]$/, '') + '/lb-v1'
    lbCore = new Hypercore(storage, { valueEncoding: 'json' })
    await lbCore.ready()

    const entries = []
    for (let i = 0; i < lbCore.length; i++) {
      try { entries.push(await lbCore.get(i)) } catch {}
    }

    send({ type: 'lb-ready', entries })

    lbSwarm = new Hyperswarm()
    lbSwarm.join(lbCore.discoveryKey, { server: true, client: true })
    lbSwarm.on('connection', (c) => {
      const isInit = c.isInitiator ?? false
      const stream = lbCore.replicate(isInit, { live: true })
      stream.pipe(c).pipe(stream)
      stream.on('error', () => {})
      c.on('error', () => {})

      lbCore.on('append', async () => {
        const fresh = []
        for (let i = 0; i < lbCore.length; i++) {
          try { fresh.push(await lbCore.get(i)) } catch {}
        }
        send({ type: 'lb-updated', entries: fresh })
      })
    })
  } catch (e) {
    console.error('[BE] lb init error:', e)
    send({ type: 'lb-ready', entries: [] })
  }
}
```

This creates a local Hypercore and replicates it with other peers using Hyperswarm!
